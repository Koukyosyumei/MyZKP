use crate::modules::algebra::field::Field;
use crate::modules::algebra::polynomial::Polynomial;
use crate::modules::merkle::merkle::Merkle;

pub struct FiatShamirTransformer {
    pub fn push(&self, obj: &vec<u8>) {

    }
    pub fn pull(&self) {

    }
}
